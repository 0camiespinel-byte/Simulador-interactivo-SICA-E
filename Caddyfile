:{$PORT:80} {
    root * /app/public
    encode zstd gzip

    # Nunca exponer configuración ni archivos internos.
    @private path /config/* /includes/* /database/* /.env /.env.*
    respond @private 404

    php_server
}